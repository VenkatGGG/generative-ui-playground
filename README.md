# Generative UI Playground

React-only generative UI platform implemented as a pnpm + Turbo monorepo.

## Tooling

- TypeScript
- Next.js App Router
- pnpm workspaces
- Turbo

## Commands

```bash
pnpm install
pnpm dev
pnpm build
pnpm test
pnpm typecheck
```
